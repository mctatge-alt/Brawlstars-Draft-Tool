# Phase 1 (data collection) dependencies only — lighter than the full requirements.txt
# (no torch / sklearn / pandas). Install:
#   pip install -r backend/requirements-collect.txt
httpx>=0.27
tenacity>=8.2
tqdm>=4.66
pydantic>=2.6
pydantic-settings>=2.2
python-dotenv>=1.0
